# CLAUDE.md

## 이번 작업: OOP 설계 점검 및 리팩토링

### 점검 기준 (블로그 "나는 정말 객체지향적으로 개발하고 있었을까?" 기준)
- 특정 Service(특히 OrderService류)에 여러 책임이 몰려있지 않은가?
  (가격 계산, 할인 계산, 재고 확인/차감, 결제, 주문 생성/상태 변경 등이 한 클래스에 다 있는지)
- 각 책임이 관련 정보를 가진 객체에게 위임되어 있는가?
  (예: 재고 관련 로직은 재고를 아는 객체가, 결제는 결제 역할을 아는 객체가)
- 구체 클래스가 아니라 역할(인터페이스)에 의존하고 있는가?
  (예: CardPayment 직접 참조 vs Payment 인터페이스 의존)
- 무조건적인 추상화/분리가 아니라, 변경 가능성이 있는 지점에만 역할을 분리했는가?

### 원칙
- 기존 동작(비즈니스 로직 결과)은 변경하지 않는다 — 순수 구조 리팩토링
- 인터페이스 추가는 "확장 가능성이 있는 지점"에만, 무분별한 분리 금지
- 리팩토링 후 기존 테스트 통과 필수 (테스트 없으면 먼저 테스트 작성)